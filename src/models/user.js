export default class User {
  // Constructor for generic user model definition - contains username, email, password
  constructor(username, email, password) {
    this.username = username;
    this.email = email;
    this.password = password;
  }
}
